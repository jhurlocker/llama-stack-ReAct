apiVersion: v1
kind: Service
metadata:
  name: {{ include "react-agent.fullname" . }}
  labels:
    {{- include "react-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "react-agent.selectorLabels" . | nindent 4 }}